import Layout from "@/components/Layout";

export const metadata = {
  title: "مقایسه با دیگر کالاها",
  robots: "noindex"
};

const CompareLayout = ({ children }) => {
  return <Layout>{children}</Layout>;
};

export default CompareLayout;
