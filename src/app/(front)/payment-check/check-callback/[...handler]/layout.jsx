export const metadata = {
    title: "check-callback",
    robots:"noindex",
  };

const CheckCallBackLayout = ({children}) => {
    return (
        <>
            {children}
        </>
    );
};

export default CheckCallBackLayout;