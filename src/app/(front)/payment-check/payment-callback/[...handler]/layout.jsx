export const metadata = {
    title: "نتیجه پرداخت شما",
    robots:"noindex",
  };

const PaymentCallBackLayout = ({children}) => {
    return (
        <>
            {children}
        </>
    );
};

export default PaymentCallBackLayout;