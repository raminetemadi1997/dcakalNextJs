export const metadata = {
    title: "quick-check-callback",
    robots:"noindex",
  };

const QuickCheckCallBackLayout = ({children}) => {
    return (
        <>
            {children}
        </>
    );
};

export default QuickCheckCallBackLayout;