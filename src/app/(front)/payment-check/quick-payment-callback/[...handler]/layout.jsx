export const metadata = {
    title: "پرداخت سریع",
    robots:"noindex",
  };

const QuickPaymentCallBackLayout = ({children}) => {
    return (
        <>
            {children}
        </>
    );
};

export default QuickPaymentCallBackLayout;