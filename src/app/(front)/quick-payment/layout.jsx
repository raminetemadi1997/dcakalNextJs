export const metadata = {
    title: "پرداخت سریع",
    robots:"noindex",
  };

const QuickPayment = ({children}) => {
    return (
        <main>
            {children}
        </main>
    );
};

export default QuickPayment;